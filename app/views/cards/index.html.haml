.container
  .main
    .inner
      %h2 購入確認画面index
      -# = link_to "カードの登録", new_card_path, class: "card-new"
      -# .buy-btn
      -#   = link_to  "/cards/#{@post.id}",class:"buy-btn__link" do
      -#     購入する
      .buy-content
        %p
          %label 販売者
          %span
            = @post.user.name
        %p
          %label 商品名
          %span
            = @post.title
        %p
          %label 価格
          %span
            = @post.price
            %span
            円
      .buy-box
        -# = link_to  "/cards/#{@post.id}",class: "login-btn" do
        -#   購入する
        = link_to  "購入する", post_path(@post.id), class: "buy-btn-last"
      -#   = link_to "購入する", pay_card_path, class: "buy-btn-last"
      .buy-card
        = link_to "カードの登録", new_card_path, class: "card-new"